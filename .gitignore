# ---- Python ----
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.hf_cache/
CLAUDE.md
.claude

# ---- Paper (tracked separately, not under git) ----
/paper

# ---- Archived / deprecated material (kept on disk, not versioned) ----
/old
/backup
/archive

# ---- Large regenerable artifacts (rebuild via the pipeline; see docs/) ----
# HuggingFace weights cache (multi-GB)
/.hf_cache
# per-(expert,dataset) embedding .npz cache (hundreds of MB/seed)
/results/cache
# all model checkpoints (experts/moe/hyperparams/zoo) -- kept local only;
# data + models to be hosted on HuggingFace (see docs/) to keep the repo lean
/models

# ---- Cluster / scratch ----
/slurm/logs
# temporary refactor-verification outputs
/results/_verify
