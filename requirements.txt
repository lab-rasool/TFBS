# Tested stack: Python 3.13, CUDA 12.8 (original experiments RTX 3090; HetMoE on H100/H200).
# Install the CUDA build of PyTorch for your system, e.g.:
#   pip install torch==2.8.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
torch>=2.8
transformers>=4.57
numpy>=2.2
pandas>=2.3
scipy>=1.16
scikit-learn>=1.8
matplotlib>=3.10
rich
# optional: per-expert Optuna HP search (skip with --use_saved_hyperparams)
optuna>=4
# optional: attribution sequence logos / sequence parsing
logomaker
biopython
captum>=0.7
